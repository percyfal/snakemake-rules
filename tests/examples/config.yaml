ngs.settings:
  db:
    ref: ref.fa

settings:
  temporary_rules:
    - bwa_index
    - bwa_mem
    - samtools_sort
    - bam_fofn
    - picard_add_or_replace_read_groups

bwa:
  mem:
    options: -M
  threads: 2
