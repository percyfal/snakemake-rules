# -*- snakemake -*-
include: "samtools.settings"

rule samtools_sam2bam:
    """Convert sam file to bam."""
    params: options = config['samtools']['options']
    input: "{prefix}.sam"
    output: "{prefix}.bam"
    shell: "samtools view {params.options} -Sb {input} > {output}"

